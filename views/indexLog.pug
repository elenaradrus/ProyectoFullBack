 
head
  link(rel='stylesheet',  href="/style.css")
  link(rel='stylesheet',  href="/style1.css")
  title Home 
  link(rel='stylesheet' href='https://unpkg.com/leaflet@1.7.1/dist/leaflet.css')
  link(rel='stylesheet' href='https://unpkg.com/leaflet-control-geocoder/dist/Control.Geocoder.css')
body  
  nav.nav
    section.textNa
     form(action="/logHome", method="post")  
      input.home(type='submit' value='Home')
     form(action="/uCuber1", method="post") 
      input.uCuber(type='submit' value='U CUBER')
     div.login
        p.busqueda2 Nº de trayecto 
        div.barra1
           input#search.search(type='search' placeholder='search')
           input#search2.search1(type='button' value='🔍')
           form(action="/logOut", method="post") 
            input#logout.log2(type='submit' value='Log out')
  section.textCentral
      h1.travel
        | Travel with us
        br
        |             Travel with CUBER
      p.travelp
        | No esperes, no desesperes
        br
        |             Con CUBER tu vida sera mas facil.
        br
        |             Somos conductores de tu tiempo y
        br
        |             no vamos a perderlo!!
      button#botonTravel.botonTravel Pide un Cuber
  section.faldon
      p.faldonP
        | Actualmente solo disponemos de coches en
        br
        |             Barcelona!
        br
        |             Pronto estaremos en tu ciudad
  section#maxBoxPedir2.maxBoxPedir2
      button#btnGr1.btngr1 X
      img.logoPedir1(src='logo.png' alt='')
      .mediumBoxPedir
        .mediumBoxPedirText
          h1.selectTouText
            | Pronto estermos en el punto de recogida
          p#livingTouText2.livingTouText
            | and start living
        #minBoxPedir2.minBoxPedir
          #puntosBoxText2.puntosBoxText
            h2#calleMap2.calleMap
            #mapaRutaIn2.mapaRutaIn
              #map2.map(style='width:100%; height:50vh; postion: relative; top: 56px')
            //- #map(style='width:100%; height: 100vh')  
                  
  section#maxBoxPedir.maxBoxPedir
      img#logoPedir.logoPedir2(src='logo.png' alt='')
      #mediumBoxPedir.mediumBoxPedir
        #mediumBoxPedirText.mediumBoxPedirText
          h1#selectTouText.selectTouText
            | Select your route
          p#livingTouText.livingTouText
            | and start living
        #minBoxPedir.minBoxPedir
          #puntosBoxText.puntosBoxText
            h2#puntosCabe.puntosCabe
              | Marca en el mapa tu recogida
            h2#calleMap.calleMap
            #mapaRutaIn.mapaRutaIn
              #map(style='width:100%; height:50vh; postion: relative; top: 56px')
            //- #map(style='width:100%; height: 100vh')  
            #pagoCuber.pagoCuber
              h2#tiempoFinal.tiempoFinal
                | Puedes tener un coche en menos de 30 min.
                br
                |                             tan solo: 20€ 
                br
                |                             Solo pagos con Tarjeta.
        form(action="/verCoche", method="post")         
          #botonpagar.botonpagar
            input#botonpagarB.botonpagarB(type='submit' value='Pedir Cuber')









  script(src='https://unpkg.com/leaflet@1.8.0/dist/leaflet.js')
  script(src='https://unpkg.com/leaflet@1.7.1/dist/leaflet.js')
  script(src='https://unpkg.com/leaflet-control-geocoder/dist/Control.Geocoder.js')
  script(src='https://unpkg.com/leaflet-routing-machine@latest/dist/leaflet-routing-machine.js')
  script(type='module' src='frontLog.js')
     